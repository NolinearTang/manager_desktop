# 🚀 快速启动指南

## 📋 系统要求

- Python 3.8+
- MySQL 5.7+ 或 PostgreSQL 12+
- 现代浏览器（Chrome、Firefox、Safari、Edge）

## ⚡ 一键启动

### 1. 启动后端服务

```bash
# 在项目根目录执行
python start_backend.py
```

或者手动启动：

```bash
cd backend
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 访问应用

- **前端页面**: http://localhost:8000/static/tag-system-list.html
- **API文档**: http://localhost:8000/docs
- **API接口**: http://localhost:8000/api/v1

## 🗄️ 数据库设置

### 1. 创建数据库

```sql
-- MySQL
CREATE DATABASE label_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- PostgreSQL
CREATE DATABASE label_system;
```

### 2. 导入数据库结构

```bash
# MySQL
mysql -u root -p label_system < database_schema.sql

# PostgreSQL
psql -U postgres -d label_system -f database_schema.sql
```

### 3. 配置数据库连接

复制 `backend/env.example` 为 `backend/.env` 并修改数据库连接信息：

```env
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/label_system
```

## 🎯 功能测试

### 1. 标签管理测试

1. 访问 http://localhost:8000/static/tag-system-list.html
2. 点击"客服对话意图体系"
3. 查看标签树结构
4. 点击不同标签查看规则列表

### 2. API接口测试

1. 访问 http://localhost:8000/docs
2. 测试各个API接口
3. 查看请求/响应格式

### 3. 意图识别测试

```bash
curl -X POST "http://localhost:8000/api/v1/intent-recognition" \
     -H "Content-Type: application/json" \
     -d '{"text": "这个E001故障码是什么意思"}'
```

## 🔧 开发模式

### 后端开发

```bash
cd backend
# 安装开发依赖
pip install -r requirements.txt

# 启动开发服务器（自动重载）
python -m uvicorn app.main:app --reload

# 运行测试
pytest tests/
```

### 前端开发

```bash
# 修改 web/ 目录下的HTML文件
# 浏览器会自动刷新（如果启用了热重载）
```

## 📊 系统架构

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端 (HTML)   │    │   后端 (FastAPI)│    │   数据库 (MySQL)│
│                 │    │                 │    │                 │
│  - 标签管理界面  │◄──►│  - REST API     │◄──►│  - 标签体系表   │
│  - 规则配置界面  │    │  - 业务逻辑服务  │    │  - 规则管理表   │
│  - 意图识别界面  │    │  - 数据模型     │    │  - 实体标签表   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 🐛 常见问题

### 1. 数据库连接失败

- 检查数据库服务是否启动
- 验证数据库连接配置
- 确认数据库用户权限

### 2. API请求失败

- 检查后端服务是否启动
- 查看浏览器控制台错误信息
- 验证API接口地址

### 3. 前端页面无法访问

- 确认后端服务正在运行
- 检查静态文件路径
- 查看服务器日志

## 📝 开发日志

- ✅ 完成数据库设计
- ✅ 实现后端API接口
- ✅ 集成前后端数据连接
- ✅ 添加意图识别功能
- ✅ 实现规则管理功能

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 创建 Pull Request

## 📞 技术支持

如有问题，请查看：
- 项目文档：README.md
- API文档：http://localhost:8000/docs
- 数据库设计：database_schema.sql

---

**标签体系管理系统** - 让意图识别更智能！ 🎉
